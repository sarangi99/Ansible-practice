---
- hosts: [webserver]
  become: yes
  
  tasks:
  - name: install apache2 on debian
    apt:
      name: apache2
      state: present
  - name: start the apache service
    service:
      name: apache2
      state: started

